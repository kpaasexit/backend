spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    bootstrap:
      enabled: false

eureka:
  client:
    enabled: false
    registerWithEureka: false
    fetchRegistry: false
  server:
    enable-self-preservation: false
  instance:
    hostname: localhost

server:
  port: 8761